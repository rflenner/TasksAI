"use client";
import { useState } from "react";
import "../accept/accept.css";
export default function LoginPage(){const[email,setEmail]=useState(""),[state,setState]=useState<"ready"|"working">("ready"),[error,setError]=useState("");const submit=async()=>{if(!email.includes("@")||state==="working")return;setState("working");setError("");const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});if(response.ok){window.location.href="/"}else{const data=await response.json();setState("ready");setError(data.error||"Could not sign you in")}};return <main className="onboard"><section className="onboard-card"><div className="onboard-logo">Task <b>AI</b></div><p>SIGN IN</p><h1>Welcome back</h1><p>Enter the email address connected to your Task AI invitation.</p><div className="onboard-form"><label className="onboard-field"><span>Email address</span><input type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} onKeyDown={event=>{if(event.key==="Enter")void submit()}} placeholder="you@company.com"/></label>{error&&<p className="onboard-error">{error}</p>}<button className="onboard-primary" disabled={state==="working"||!email.includes("@")} onClick={submit}>{state==="working"?"Signing in…":"Sign in"}</button></div></section></main>}
