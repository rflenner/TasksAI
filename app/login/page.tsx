"use client";
import { useState } from "react";
import "../accept/accept.css";
export default function LoginPage(){const[email,setEmail]=useState(""),[state,setState]=useState<"ready"|"working"|"sent">("ready"),[error,setError]=useState("");const submit=async()=>{setState("working");setError("");const response=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email})});const data=await response.json();if(response.ok)setState("sent");else{setState("ready");setError(data.error||"Could not send the sign-in link")}};return <main className="onboard"><section className="onboard-card"><div className="onboard-logo">Task <b>AI</b></div><p>SECURE SIGN-IN</p><h1>Welcome back</h1>{state==="sent"?<><span className="success-mark">✓</span><p>If your invited account is active, a one-time sign-in link is on its way. Check your inbox.</p></>:<><p>Enter the verified email address connected to your Task AI invitation.</p><div className="onboard-form"><label className="onboard-field"><span>Email address</span><input type="email" autoComplete="email" value={email} onChange={event=>setEmail(event.target.value)} placeholder="you@company.com"/></label>{error&&<p className="onboard-error">{error}</p>}<button className="onboard-primary" disabled={state==="working"||!email.includes("@")} onClick={submit}>{state==="working"?"Sending secure link…":"Email me a sign-in link"}</button></div></>}</section></main>}
