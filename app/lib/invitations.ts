export function validateInvitationProfile(input:{firstName?:string;lastName?:string;company?:string;email?:string},invitedEmail:string){const firstName=String(input.firstName||"").trim(),lastName=String(input.lastName||"").trim(),company=String(input.company||"").trim(),email=String(input.email||"").trim().toLowerCase();if(!firstName||!lastName||!company)return{error:"First name, last name, and company are required"} as const;if(email!==invitedEmail.toLowerCase())return{error:"Use the email address that received the invitation"} as const;return{value:{firstName,lastName,company,email,name:`${firstName} ${lastName}`}} as const}
